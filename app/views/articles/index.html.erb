<% @articles.each do |article| %>
<ul>
  <li>
    <h2><%= link_to "#{article.title}", article_path(article.id), class:"text-decoration-none" %></h2>
    <p><%= article.content.first(50) %>...</p>
    </li>
</ul>
<% end %>
